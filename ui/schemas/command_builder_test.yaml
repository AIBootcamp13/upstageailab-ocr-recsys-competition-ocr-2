title: "OCR Testing Command Builder"

# UI schema for Streamlit command builder. Only for UI input validation and command building.
constant_overrides: []

ui_elements:
  - key: exp_name
    type: text_input
    label: "Experiment Name"
    default: "ocr_testing"
    hydra_override: "exp_name"
    validation:
      required: true
      min_length: 3

  - key: checkpoint_path
    type: selectbox
    label: "Checkpoint Path"
    options_source: "checkpoints"
    default: ""
    hydra_override: "checkpoint_path"
    validation:
      required: true
